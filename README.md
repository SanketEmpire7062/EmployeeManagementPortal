# EmployeeManagementPortal
EmployeeManagementPortal is a Spring Boot + MySQL application that provides a basic Employee Management module with CRUD operations, validations, soft deletes, filtering, pagination, and sorting. It supports unique email validation, department-based analytics, salary-based queries, and epoch-based date handling with readable API responses.
